# Phase 7: Release Pipeline Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship `pkgr` v1.0.0. Build signed binaries via goreleaser; publish to homebrew tap, scoop bucket, winget manifest, .deb / .rpm; host install.sh; run e2e Docker matrix nightly in CI.

**Architecture:** All packaging driven from `.goreleaser.yaml`. Signing via cosign keyless OIDC (GitHub-Actions identity). Channel-specific manifests live in side repos:
- `homebrew-pkgr` (tap)
- `scoop-pkgr` (bucket)
- `pkgr-winget` (winget PRs)

`Makefile release` runs goreleaser locally with `--snapshot` for dry-runs.

**Tech Stack:** [goreleaser](https://goreleaser.com), [nfpm](https://github.com/goreleaser/nfpm), [cosign](https://github.com/sigstore/cosign), [syft](https://github.com/anchore/syft), GitHub Actions.

---

## File Structure

```
.goreleaser.yaml
.github/workflows/release.yml
.github/workflows/e2e.yml
tests/e2e/docker/{ubuntu.Dockerfile, fedora.Dockerfile, arch.Dockerfile}
tests/e2e/scripts/{ubuntu_smoke.sh, fedora_smoke.sh, arch_smoke.sh}
scripts/install.sh
```

---

### Task 1: goreleaser config

**Files:**
- Create: `.goreleaser.yaml`

- [ ] **Step 1: Write `.goreleaser.yaml`**

```yaml
version: 2
project_name: pkgr

before:
  hooks:
    - go mod tidy

builds:
  - id: pkgr
    main: ./cmd/pkgr
    binary: pkgr
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w
      - -X main.version={{ .Version }}
      - -X main.commit={{ .Commit }}
      - -X main.date={{ .Date }}
    goos: [linux, darwin, windows]
    goarch: [amd64, arm64]
    ignore:
      - goos: windows
        goarch: arm64

archives:
  - id: pkgr-archive
    builds: [pkgr]
    name_template: "pkgr_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        format: zip
    files:
      - LICENSE
      - README.md
      - CLAUDE.md

checksum:
  name_template: "checksums.txt"
  algorithm: sha256

signs:
  - cmd: cosign
    artifacts: checksum
    output: true
    args:
      - "sign-blob"
      - "--yes"
      - "--output-signature=${signature}"
      - "${artifact}"

sboms:
  - artifacts: archive

nfpms:
  - id: pkgr-pkg
    package_name: pkgr
    file_name_template: "pkgr_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    homepage: https://github.com/ramtinhoss/pkgr
    maintainer: Ramtin Hosseini <ramtinhoss@gmail.com>
    description: Cross-platform package manager TUI/CLI
    license: MIT
    formats: [deb, rpm]
    bindir: /usr/bin

brews:
  - name: pkgr
    repository:
      owner: ramtinhoss
      name: homebrew-pkgr
      branch: main
    homepage: https://github.com/ramtinhoss/pkgr
    description: One TUI/CLI for brew, apt, dnf, pacman, snap, npm, pip, cargo, …
    install: |
      bin.install "pkgr"
    test: |
      system "#{bin}/pkgr", "version"

scoops:
  - name: pkgr
    repository:
      owner: ramtinhoss
      name: scoop-pkgr
      branch: main
    homepage: https://github.com/ramtinhoss/pkgr
    description: Cross-platform package manager TUI/CLI
    license: MIT

release:
  github:
    owner: ramtinhoss
    name: pkgr
  draft: false
  prerelease: auto
  name_template: "v{{ .Version }}"

changelog:
  sort: asc
  use: github
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^ci:"
      - "^chore:"
```

- [ ] **Step 2: Validate**

```bash
goreleaser check
goreleaser release --snapshot --clean --skip=publish,sign
ls -la dist/
```
Expected: archives for linux/darwin/windows × amd64/arm64; deb + rpm.

- [ ] **Step 3: Commit**

```bash
git add .goreleaser.yaml
git commit -m "ci: add goreleaser config for cross-platform release"
```

---

### Task 2: Release workflow

**Files:**
- Create: `.github/workflows/release.yml`

- [ ] **Step 1: Write workflow**

```yaml
name: release
on:
  push:
    tags: ["v*"]

permissions:
  contents: write
  id-token: write  # cosign keyless

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-go@v5
        with:
          go-version: "1.22"
      - uses: sigstore/cosign-installer@v3
      - uses: anchore/sbom-action/download-syft@v0
      - uses: goreleaser/goreleaser-action@v6
        with:
          version: latest
          args: release --clean
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          HOMEBREW_TAP_GITHUB_TOKEN: ${{ secrets.TAP_TOKEN }}
          SCOOP_BUCKET_GITHUB_TOKEN: ${{ secrets.SCOOP_TOKEN }}
```

- [ ] **Step 2: Document required secrets in `README.md`**

Append a section:

```markdown
## Release Secrets (maintainer only)

- `TAP_TOKEN` — fine-grained PAT for `ramtinhoss/homebrew-pkgr`
- `SCOOP_TOKEN` — fine-grained PAT for `ramtinhoss/scoop-pkgr`
- (winget PRs use a GitHub App via the winget-releaser action; configure separately)
```

- [ ] **Step 3: Commit**

```bash
git add .github/workflows/release.yml README.md
git commit -m "ci(release): goreleaser-driven release on tag push"
```

---

### Task 3: winget manifest workflow

**Files:**
- Create: `.github/workflows/winget.yml`

- [ ] **Step 1: Write workflow**

```yaml
name: winget
on:
  release:
    types: [released]

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: vedantmgoyal2009/winget-releaser@v2
        with:
          identifier: RamtinHoss.pkgr
          installers-regex: 'pkgr_.*_windows_.*\.zip$'
          token: ${{ secrets.WINGET_TOKEN }}
```

- [ ] **Step 2: Commit**

```bash
git add .github/workflows/winget.yml
git commit -m "ci(winget): auto-PR manifest to microsoft/winget-pkgs on release"
```

---

### Task 4: E2E Docker matrix (Linux distros)

**Files:**
- Create: `tests/e2e/docker/ubuntu.Dockerfile`
- Create: `tests/e2e/docker/fedora.Dockerfile`
- Create: `tests/e2e/docker/arch.Dockerfile`
- Create: `tests/e2e/scripts/{ubuntu_smoke.sh, fedora_smoke.sh, arch_smoke.sh}`
- Create: `.github/workflows/e2e.yml`
- Modify: `Makefile` (add `e2e` target)

- [ ] **Step 1: Write `tests/e2e/docker/ubuntu.Dockerfile`**

```dockerfile
FROM ubuntu:24.04
ENV DEBIAN_FRONTEND=noninteractive
RUN apt-get update && apt-get install -y --no-install-recommends \
        curl ca-certificates sudo build-essential \
        nodejs npm python3 python3-pip pipx \
        && rm -rf /var/lib/apt/lists/*
COPY ./pkgr /usr/local/bin/pkgr
COPY ./tests/e2e/scripts/ubuntu_smoke.sh /smoke.sh
RUN chmod +x /usr/local/bin/pkgr /smoke.sh
ENTRYPOINT ["/smoke.sh"]
```

- [ ] **Step 2: Write `tests/e2e/scripts/ubuntu_smoke.sh`**

```bash
#!/usr/bin/env bash
set -euo pipefail
echo "=== pkgr version ==="
pkgr version

echo "=== pkgr pm list ==="
pkgr pm list

echo "=== pkgr search node (npm + apt) ==="
pkgr search node --limit 5

echo "=== pkgr list ==="
pkgr list --limit 10 || true

echo "=== pkgr --dry-run install ripgrep@apt ==="
pkgr --dry-run install ripgrep@apt

echo "ubuntu smoke OK"
```

- [ ] **Step 3: Write `tests/e2e/docker/fedora.Dockerfile`** + script

```dockerfile
FROM fedora:40
RUN dnf install -y nodejs npm python3 python3-pip pipx git \
    && dnf clean all
COPY ./pkgr /usr/local/bin/pkgr
COPY ./tests/e2e/scripts/fedora_smoke.sh /smoke.sh
RUN chmod +x /usr/local/bin/pkgr /smoke.sh
ENTRYPOINT ["/smoke.sh"]
```

`tests/e2e/scripts/fedora_smoke.sh`:
```bash
#!/usr/bin/env bash
set -euo pipefail
pkgr version
pkgr pm list
pkgr search ripgrep --pm dnf --limit 5
pkgr --dry-run install ripgrep@dnf
echo "fedora smoke OK"
```

- [ ] **Step 4: Write `tests/e2e/docker/arch.Dockerfile`** + script

```dockerfile
FROM archlinux:latest
RUN pacman -Syu --noconfirm nodejs npm python python-pip git \
    && pacman -Scc --noconfirm
COPY ./pkgr /usr/local/bin/pkgr
COPY ./tests/e2e/scripts/arch_smoke.sh /smoke.sh
RUN chmod +x /usr/local/bin/pkgr /smoke.sh
ENTRYPOINT ["/smoke.sh"]
```

`tests/e2e/scripts/arch_smoke.sh`:
```bash
#!/usr/bin/env bash
set -euo pipefail
pkgr version
pkgr pm list
pkgr search ripgrep --pm pacman --limit 5
pkgr --dry-run install ripgrep@pacman
echo "arch smoke OK"
```

- [ ] **Step 5: Add `Makefile` target**

```makefile
.PHONY: e2e
e2e: build
	for d in ubuntu fedora arch; do \
	  echo "=== e2e $$d ==="; \
	  docker build -t pkgr-e2e-$$d -f tests/e2e/docker/$$d.Dockerfile .; \
	  docker run --rm pkgr-e2e-$$d; \
	done
```

- [ ] **Step 6: Write `.github/workflows/e2e.yml`**

```yaml
name: e2e
on:
  schedule:
    - cron: "0 7 * * *"  # nightly UTC
  workflow_dispatch:
  pull_request:
    paths:
      - "internal/manager/**"
      - "internal/runner/**"
      - "internal/orchestrator/**"
      - "tests/e2e/**"
      - ".github/workflows/e2e.yml"

jobs:
  linux:
    strategy:
      matrix:
        distro: [ubuntu, fedora, arch]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with: { go-version: "1.22" }
      - run: make build
      - run: |
          docker build -t pkgr-e2e-${{ matrix.distro }} \
            -f tests/e2e/docker/${{ matrix.distro }}.Dockerfile .
          docker run --rm pkgr-e2e-${{ matrix.distro }}

  macos:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with: { go-version: "1.22" }
      - run: make build
      - run: |
          ./pkgr version
          ./pkgr pm list
          ./pkgr search ripgrep --limit 5
          ./pkgr --dry-run install ripgrep@brew

  windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with: { go-version: "1.22" }
      - run: go build -o pkgr.exe ./cmd/pkgr
      - run: .\pkgr.exe version
      - run: .\pkgr.exe pm list
      - run: .\pkgr.exe --dry-run install ripgrep@scoop || echo no scoop
```

- [ ] **Step 7: Commit**

```bash
git add tests/e2e/ .github/workflows/e2e.yml Makefile
git commit -m "ci(e2e): docker matrix for ubuntu/fedora/arch + macOS/Windows smoke"
```

---

### Task 5: install.sh

**Files:**
- Create: `scripts/install.sh`

- [ ] **Step 1: Write installer**

```bash
#!/usr/bin/env bash
# Install latest pkgr binary into /usr/local/bin (or $PKGR_INSTALL_DIR).
set -euo pipefail

REPO="ramtinhoss/pkgr"
INSTALL_DIR="${PKGR_INSTALL_DIR:-/usr/local/bin}"

uname_s=$(uname -s)
uname_m=$(uname -m)

case "$uname_s" in
  Linux)   os=linux ;;
  Darwin)  os=darwin ;;
  *) echo "unsupported OS: $uname_s" >&2; exit 1 ;;
esac
case "$uname_m" in
  x86_64|amd64) arch=amd64 ;;
  aarch64|arm64) arch=arm64 ;;
  *) echo "unsupported arch: $uname_m" >&2; exit 1 ;;
esac

# Resolve latest release tag.
tag=$(curl -fsSL "https://api.github.com/repos/${REPO}/releases/latest" | \
      sed -n 's/.*"tag_name": *"\([^"]*\)".*/\1/p' | head -n1)
if [ -z "$tag" ]; then
  echo "could not resolve latest tag" >&2; exit 1
fi
ver="${tag#v}"

archive="pkgr_${ver}_${os}_${arch}.tar.gz"
url="https://github.com/${REPO}/releases/download/${tag}/${archive}"

tmp=$(mktemp -d)
trap "rm -rf '$tmp'" EXIT

echo "downloading $url"
curl -fsSL "$url" -o "$tmp/$archive"

# Verify checksum.
sums_url="https://github.com/${REPO}/releases/download/${tag}/checksums.txt"
curl -fsSL "$sums_url" -o "$tmp/checksums.txt"
( cd "$tmp" && grep "$archive" checksums.txt | sha256sum -c - )

# Extract + install.
tar -xzf "$tmp/$archive" -C "$tmp"

if [ -w "$INSTALL_DIR" ]; then
  mv "$tmp/pkgr" "$INSTALL_DIR/pkgr"
else
  sudo mv "$tmp/pkgr" "$INSTALL_DIR/pkgr"
fi
chmod +x "$INSTALL_DIR/pkgr"

echo "installed: $($INSTALL_DIR/pkgr version | head -1)"
```

- [ ] **Step 2: Smoke test locally** (no real network call required; verify shellcheck):

```bash
shellcheck scripts/install.sh
```
Expected: no errors. Warnings about `SC2086` (word splitting) acceptable for the controlled inputs above.

- [ ] **Step 3: Commit**

```bash
git add scripts/install.sh
git commit -m "feat(install): self-installer script with sha256 verification"
```

---

### Task 6: Cut v1.0.0

**Files:**
- (none — git tag + push only)

- [ ] **Step 1: Verify clean tree + green CI**

```bash
git status
git log --oneline -5
gh run list -L 5
```

- [ ] **Step 2: Tag**

```bash
git tag -a v1.0.0 -m "pkgr v1.0.0"
git push origin v1.0.0
```

- [ ] **Step 3: Watch release workflow**

```bash
gh run watch
```
Expected: goreleaser builds, signs, publishes; brew + scoop taps PR'd; winget workflow triggers.

- [ ] **Step 4: Smoke-install on a clean machine**

```bash
curl -fsSL https://raw.githubusercontent.com/ramtinhoss/pkgr/v1.0.0/scripts/install.sh | sh
pkgr version
pkgr pm list
```

- [ ] **Step 5: Update README badges + install one-liners**

Edit `README.md` install section to include the verified one-liners:

```markdown
## Install

macOS / Linux:
```
brew install ramtinhoss/pkgr/pkgr
# or
curl -fsSL https://raw.githubusercontent.com/ramtinhoss/pkgr/main/scripts/install.sh | sh
```

Windows:
```
scoop bucket add pkgr https://github.com/ramtinhoss/scoop-pkgr
scoop install pkgr
# or
winget install RamtinHoss.pkgr
```
```

Commit:

```bash
git add README.md
git commit -m "docs(readme): post-release install one-liners"
git push
```

---

## Phase 7 Acceptance

- `git tag v1.0.0 && git push` triggers release pipeline end-to-end
- Release page contains signed archives + checksums + SBOMs + .deb + .rpm
- `brew install ramtinhoss/pkgr/pkgr` works on macOS + Linux
- `scoop install pkgr` works on Windows after `scoop bucket add`
- `winget install RamtinHoss.pkgr` works after manifest PR merges into `microsoft/winget-pkgs`
- `curl -fsSL .../install.sh | sh` installs on Linux + macOS, verifies sha256
- Nightly e2e workflow stays green across ubuntu/fedora/arch + macOS + windows
- `make e2e` runs locally end-to-end and finishes < 5 min on a developer laptop
